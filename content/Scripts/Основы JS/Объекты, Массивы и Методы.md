## 🏗️ Объекты (Objects)

### Что такое объект?
Объект — это коллекция пар **ключ: значение**, где ключ — это строка (называется свойством), а значение может быть любым типом данных.

### Создание объектов:

```javascript
// 1. Литерал объекта (самый частый способ)
const user = {
    name: "Анна",
    age: 25,
    isAdmin: false,
    "любимый цвет": "синий" // ключ с пробелом в кавычках
};

// 2. Через конструктор Object
const car = new Object();
car.brand = "Toyota";
car.model = "Camry";

// 3. Object.create()
const proto = { greeting: "Привет" };
const obj = Object.create(proto);
```

### Доступ к свойствам:
```javascript
const user = {
    name: "Иван",
    age: 30
};

// Точечная нотация (для стандартных ключей)
console.log(user.name); // "Иван"
console.log(user.age);  // 30

// Скобочная нотация (для всех ключей, включая вычисляемые)
console.log(user["name"]); // "Иван"

const key = "age";
console.log(user[key]); // 30 (вычисляемое свойство)

// Для ключей с пробелами ТОЛЬКО скобочная нотация
user["любимый цвет"] = "зеленый";
```

### Методы объектов:
```javascript
const person = {
    firstName: "Мария",
    lastName: "Петрова",
    
    // Метод — функция внутри объекта
    getFullName: function() {
        return `${this.firstName} ${this.lastName}`;
    },
    
    // Современный синтаксис ES6
    greet() {
        console.log(`Привет, я ${this.firstName}`);
    }
};

person.greet(); // "Привет, я Мария"
```

### Деструктуризация объектов (ES6+):
```javascript
const options = {
    width: 100,
    height: 200,
    title: "Меню"
};

// Извлечение свойств в переменные
const { width, height, title } = options;
console.log(width);  // 100
console.log(height); // 200

// С другим именем переменной
const { width: w, height: h } = options;

// Значения по умолчанию
const { width = 150, theme = "dark" } = options;
```

## 📋 Массивы (Arrays)

### Создание массивов:
```javascript
// 1. Литерал массива
const fruits = ["яблоко", "банан", "апельсин"];

// 2. Через конструктор Array
const numbers = new Array(1, 2, 3);

// 3. Массив с разными типами данных
const mixed = [1, "текст", true, { name: "объект" }, [1, 2, 3]];

// 4. Пустой массив заданной длины
const emptyArray = new Array(5); // [empty × 5]
```

### Основные операции с массивами:
```javascript
const colors = ["красный", "зеленый"];

// Добавление элементов
colors.push("синий");     // в конец → ["красный", "зеленый", "синий"]
colors.unshift("желтый"); // в начало → ["желтый", "красный", "зеленый", "синий"]

// Удаление элементов
colors.pop();            // с конца → удаляет "синий"
colors.shift();          // с начала → удаляет "желтый"

// Доступ к элементам
console.log(colors[0]);   // "красный"
console.log(colors.length); // 2

// Изменение элемента
colors[1] = "фиолетовый"; // ["красный", "фиолетовый"]
```

## 🎯 Важные методы массивов (функциональные методы)

### 1. **map()** — преобразование каждого элемента
Создаёт новый массив, применяя функцию к каждому элементу.

```javascript
const numbers = [1, 2, 3, 4];

// Умножить каждый элемент на 2
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8]

// Преобразовать числа в строки
const strings = numbers.map(num => `Число: ${num}`);
console.log(strings); // ["Число: 1", "Число: 2", "Число: 3", "Число: 4"]
```

### 2. **filter()** — фильтрация элементов
Создаёт новый массив с элементами, прошедшими проверку.

```javascript
const numbers = [1, 2, 3, 4, 5, 6];

// Только четные числа
const even = numbers.filter(num => num % 2 === 0);
console.log(even); // [2, 4, 6]

// Числа больше 3
const greaterThan3 = numbers.filter(num => num > 3);
console.log(greaterThan3); // [4, 5, 6]

// Поиск объектов
const users = [
    { name: "Анна", age: 25 },
    { name: "Иван", age: 17 },
    { name: "Мария", age: 30 }
];

const adults = users.filter(user => user.age >= 18);
console.log(adults); // [{name: "Анна", age: 25}, {name: "Мария", age: 30}]
```

### 3. **reduce()** — аккумулятор значений
Преобразует массив в одно значение (число, строку, объект).

```javascript
const numbers = [1, 2, 3, 4, 5];

// Сумма всех чисел
const sum = numbers.reduce((accumulator, current) => {
    return accumulator + current;
}, 0); // начальное значение accumulator = 0
console.log(sum); // 15

// Можно короче:
const sumShort = numbers.reduce((acc, cur) => acc + cur, 0);

// Поиск максимального значения
const max = numbers.reduce((acc, cur) => Math.max(acc, cur));
console.log(max); // 5

// Группировка объектов
const items = [
    { type: "fruit", name: "яблоко" },
    { type: "vegetable", name: "морковь" },
    { type: "fruit", name: "банан" }
];

const grouped = items.reduce((acc, item) => {
    if (!acc[item.type]) {
        acc[item.type] = [];
    }
    acc[item.type].push(item.name);
    return acc;
}, {});

console.log(grouped);
// {
//   fruit: ["яблоко", "банан"],
//   vegetable: ["морковь"]
// }
```

### 4. **forEach()** — перебор элементов
Выполняет функцию для каждого элемента (не создаёт новый массив).

```javascript
const fruits = ["яблоко", "банан", "апельсин"];

fruits.forEach((fruit, index) => {
    console.log(`${index + 1}. ${fruit}`);
});
// 1. яблоко
// 2. банан
// 3. апельсин
```

### 5. **find() и findIndex()** — поиск элементов
```javascript
const users = [
    { id: 1, name: "Анна" },
    { id: 2, name: "Иван" },
    { id: 3, name: "Мария" }
];

// Найти пользователя по id
const user = users.find(u => u.id === 2);
console.log(user); // { id: 2, name: "Иван" }

// Найти индекс элемента
const index = users.findIndex(u => u.name === "Мария");
console.log(index); // 2
```

### 6. **some() и every()** — проверка условий
```javascript
const numbers = [1, 2, 3, 4, 5];

// some() - хотя бы один элемент удовлетворяет условию
const hasEven = numbers.some(num => num % 2 === 0);
console.log(hasEven); // true

// every() - все элементы удовлетворяют условию
const allPositive = numbers.every(num => num > 0);
console.log(allPositive); // true
```

### 7. **sort()** — сортировка массива
```javascript
const numbers = [3, 1, 4, 1, 5, 9];

// Сортировка чисел (по умолчанию как строк!)
numbers.sort((a, b) => a - b); // по возрастанию
console.log(numbers); // [1, 1, 3, 4, 5, 9]

numbers.sort((a, b) => b - a); // по убыванию
console.log(numbers); // [9, 5, 4, 3, 1, 1]

// Сортировка строк
const fruits = ["яблоко", "банан", "Апельсин", "арбуз"];
fruits.sort(); // учитывает регистр!
console.log(fruits); // ["Апельсин", "арбуз", "банан", "яблоко"]

// Без учета регистра
fruits.sort((a, b) => a.localeCompare(b, 'ru'));
```

## 🔗 JSON (JavaScript Object Notation)

Формат обмена данными, основанный на синтаксисе JavaScript.

```javascript
const user = {
    name: "Иван",
    age: 30,
    skills: ["JS", "HTML", "CSS"]
};

// Преобразование объекта в JSON-строку
const jsonString = JSON.stringify(user);
console.log(jsonString);
// '{"name":"Иван","age":30,"skills":["JS","HTML","CSS"]}'

// Преобразование JSON-строки в объект
const parsedUser = JSON.parse(jsonString);
console.log(parsedUser.name); // "Иван"

// Параметры stringify
const prettyJson = JSON.stringify(user, null, 2);
console.log(prettyJson); // Красивое форматирование с отступами
```


## 💡 Советы по обучению:

1. **Методы цепочкой:**
   ```javascript
   const result = users
       .filter(user => user.age >= 18)
       .map(user => user.name)
       .sort();
   ```

2. **Используйте деструктуризацию:**
   ```javascript
   users.forEach(({ name, age }) => {
       console.log(`${name}, ${age} лет`);
   });
   ```

3. **Опциональная цепочка (optional chaining) ES2020:**
   ```javascript
   // Безопасный доступ к вложенным свойствам
   const city = user?.address?.city || "Не указан";
   ```

4. **Объединение с spread/rest операторами:**
   ```javascript
   // Копирование массива
   const newArray = [...oldArray];
   
   // Копирование объекта
   const newObj = { ...oldObj, newProperty: "value" };
   
   // Rest параметры
   const [first, ...rest] = [1, 2, 3, 4];
   ```

## 📚 Что изучать дальше?

После освоения объектов и массивов:
1. **Работа с DOM** — взаимодействие с HTML-страницей
2. **События (Events)** — обработка кликов, ввода и т.д.
3. **Асинхронность** — Promise, async/await, работа с API

---

**Важно:** Решайте задачи самостоятельно! Попробуйте написать функции для заданий выше без подглядывания. Практика — единственный способ научиться программировать.