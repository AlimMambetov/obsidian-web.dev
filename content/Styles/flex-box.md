# 📦 Flexbox - Краткий справочник

## 🎯 Основы

### Создание контейнера:
```css
.container {
  display: flex; /* или inline-flex */
}
```

## 📍 Направление элементов

### Свойство `flex-direction`:
| Значение | Описание | Визуализация |
|----------|----------|--------------|
| `row` | Слева направо | `[1][2][3] →` |
| `row-reverse` | Справа налево | `← [3][2][1]` |
| `column` | Сверху вниз | `↓ [1] [2] [3]` |
| `column-reverse` | Снизу вверх | `↑ [3] [2] [1]` |

```css
.container {
  flex-direction: row; /* по умолчанию */
}
```

## 🔄 Перенос элементов

### Свойство `flex-wrap`:
| Значение | Описание |
|----------|----------|
| `nowrap` | Все в одну строку (по умолчанию) |
| `wrap` | Перенос на новую строку |
| `wrap-reverse` | Перенос в обратном порядке |

```css
.container {
  flex-wrap: wrap;
}
```

### Короткая запись:
```css
.container {
  flex-flow: row wrap; /* direction + wrap */
}
```

## ↔️ Выравнивание по главной оси

### Свойство `justify-content`:
| Значение | Описание | Пример |
|----------|----------|--------|
| `flex-start` | В начале (по умолчанию) | `[🍎][🍌][🍒]_________` |
| `flex-end` | В конце | `_________[🍎][🍌][🍒]` |
| `center` | По центру | `____[🍎][🍌][🍒]____` |
| `space-between` | Равномерно, без отступов по краям | `[🍎]__[🍌]__[🍒]` |
| `space-around` | Равномерно, с отступами по краям | `_[🍎]_[🍌]_[🍒]_` |
| `space-evenly` | Полностью равномерно | `_[🍎]_[🍌]_[🍒]_` |

```css
.container {
  justify-content: center;
}
```

## ↕️ Выравнивание по поперечной оси

### Свойство `align-items`:
| Значение | Описание |
|----------|----------|
| `stretch` | Растянуть (по умолчанию) |
| `flex-start` | В начале |
| `flex-end` | В конце |
| `center` | По центру |
| `baseline` | По базовой линии текста |

```css
.container {
  align-items: center;
}
```

### Для многострочных контейнеров:
```css
.container {
  flex-wrap: wrap;
  align-content: space-between; /* выравнивание строк */
}
```

## 📦 Свойства элементов

### Размер элементов `flex`:
| Запись | Расшифровка | Использование |
|--------|-------------|---------------|
| `flex: 1` | `1 1 0%` | Занимает свободное место |
| `flex: 0 0 200px` | `0 0 200px` | Фиксированный размер 200px |
| `flex: auto` | `1 1 auto` | По размеру контента + растяжение |
| `flex: none` | `0 0 auto` | Только по размеру контента |

```css
.item {
  flex: 1; /* растягивается */
}

.sidebar {
  flex: 0 0 250px; /* фиксированная ширина */
}
```

### Индивидуальное выравнивание:
```css
.item {
  align-self: flex-end; /* переопределяет align-items */
}
```

### Порядок элементов:
```css
.item1 { order: 2; } /* будет вторым */
.item2 { order: 1; } /* будет первым */
.item3 { order: 3; } /* будет третьим */
```

## 🎯 Практические примеры

### 1. Центрирование:
```css
.center {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}
```

### 2. Навигация:
```css
.nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
```

### 3. Адаптивные карточки:
```css
.cards {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.card {
  flex: 1 1 300px; /* мин. ширина 300px */
}
```

### 4. Две колонки:
```css
.layout {
  display: flex;
  min-height: 100vh;
}

.sidebar {
  flex: 0 0 250px;
}

.content {
  flex: 1;
}
```

### 5. Прижать футер к низу:
```css
.page {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.main {
  flex: 1; /* занимает всё место */
}
```

## 🔧 Отступы между элементами

### Свойство `gap`:
```css
.container {
  display: flex;
  gap: 20px; /* отступы между элементами */
  
  /* или отдельно: */
  row-gap: 10px;
  column-gap: 15px;
}
```

## 📝 Шпаргалка по выравниванию

### Для `row`:
```
justify-content → горизонталь (основная)
align-items    ↓ вертикаль (поперечная)
```

### Для `column`:
```
justify-content ↓ вертикаль (основная)
align-items    → горизонталь (поперечная)
```

## 🚀 Быстрые решения

### Равномерное распределение:
```css
.container {
  display: flex;
  justify-content: space-between;
}
```

### Резиновые колонки:
```css
.item { flex: 1; }      /* одинаковая ширина */
.wide { flex: 2; }      /* в 2 раза шире */
.narrow { flex: 0.5; }  /* в 2 раза уже */
```

### Карточки в сетке:
```css
.grid {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.grid-item {
  flex: 1 1 calc(33.333% - 20px);
}
```

## 💡 Подсказки

- Используйте `gap` вместо `margin` для отступов
- `flex: 1` — лучший друг для резиновых элементов
- Центрирование: `justify-content: center + align-items: center`
- Для адаптивности добавляйте `flex-wrap: wrap`