# 📱 Адаптивный дизайн - Краткий справочник

## 🎯 Медиазапросы

### Базовый синтаксис:
```css
@media (условие) {
  /* стили */
}
```

## 📏 Основные условия

### По ширине экрана:
| Запрос | Описание |
|--------|----------|
| `min-width` | Минимальная ширина |
| `max-width` | Максимальная ширина |
| `width` | Конкретная ширина |

```css
/* Mobile First подход: */
.element { 
  /* стили для мобильных */
}

@media (min-width: 768px) {
  .element {
    /* стили для планшетов и больше */
  }
}

@media (min-width: 1024px) {
  .element {
    /* стили для десктопа */
  }
}
```

### Типовые брейкпоинты:
```css
/* Мобильные: до 767px */
/* Планшеты: 768px - 1023px */
/* Десктоп: от 1024px */

@media (min-width: 768px) { /* планшет и больше */ }
@media (min-width: 1024px) { /* десктоп */ }
@media (min-width: 1280px) { /* широкий экран */ }
```

## 📱 Разные типы экранов

### По ориентации:
```css
@media (orientation: portrait) {
  /* вертикальная ориентация */
}

@media (orientation: landscape) {
  /* горизонтальная ориентация */
}
```

### По плотности пикселей:
```css
@media (min-resolution: 2dppx) {
  /* ретина-экраны */
}

@media (min-resolution: 192dpi) {
  /* высокое DPI */
}
```

### По возможностям:
```css
@media (hover: hover) {
  /* устройства с мышью/ховером */
}

@media (pointer: coarse) {
  /* сенсорные устройства */
}

@media (pointer: fine) {
  /* устройства с мышью */
}
```

## 🎨 Адаптивные изображения

### HTML подход:
```html
<picture>
  <source media="(min-width: 1024px)" srcset="large.jpg">
  <source media="(min-width: 768px)" srcset="medium.jpg">
  <img src="small.jpg" alt="Описание">
</picture>
```

### CSS подход:
```css
.background {
  background-image: url('small.jpg');
}

@media (min-width: 768px) {
  .background {
    background-image: url('medium.jpg');
  }
}

@media (min-width: 1024px) {
  .background {
    background-image: url('large.jpg');
  }
}
```

### Атрибут srcset:
```html
<img 
  src="image-800.jpg"
  srcset="image-400.jpg 400w,
          image-800.jpg 800w,
          image-1200.jpg 1200w"
  sizes="(max-width: 600px) 100vw,
         (max-width: 1200px) 50vw,
         33vw"
  alt="Адаптивное изображение"
>
```

## 🔄 Адаптивная типографика

### Относительные единицы:
| Единица | Описание | Пример |
|---------|----------|--------|
| `rem` | Относительно html | `font-size: 1rem;` |
| `em` | Относительно родителя | `padding: 1em;` |
| `vw` | % ширины окна | `font-size: 4vw;` |
| `vh` | % высоты окна | `height: 100vh;` |
| `%` | % от родителя | `width: 50%;` |

### Адаптивный шрифт:
```css
html {
  font-size: 16px; /* база */
}

@media (min-width: 768px) {
  html {
    font-size: 18px;
  }
}

@media (min-width: 1024px) {
  html {
    font-size: 20px;
  }
}
```

### Жидкая типографика:
```css
h1 {
  font-size: clamp(1.5rem, 4vw, 3rem);
}

p {
  font-size: clamp(1rem, 2vw, 1.25rem);
}
```

## 📦 Адаптивные контейнеры

### Адаптивный контейнер:
```css
.container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 15px;
}

@media (min-width: 768px) {
  .container {
    padding: 0 30px;
  }
}
```

### Жидкие макеты:
```css
.layout {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.column {
  flex: 1 1 100%; /* мобильные: 100% */
}

@media (min-width: 768px) {
  .column {
    flex: 1 1 50%; /* планшеты: 50% */
  }
}

@media (min-width: 1024px) {
  .column {
    flex: 1 1 33.333%; /* десктоп: 33.3% */
  }
}
```

## 🎯 Скрытие/показ элементов

```css
.mobile-only {
  display: block;
}

.desktop-only {
  display: none;
}

@media (min-width: 768px) {
  .mobile-only {
    display: none;
  }
  
  .desktop-only {
    display: block;
  }
}
```

## 📱 Viewport метатег

### Обязательный:
```html
<meta name="viewport" content="width=device-width, initial-scale=1">
```

### Расширенные настройки:
```html
<meta name="viewport" content="
  width=device-width,
  initial-scale=1,
  maximum-scale=5,
  minimum-scale=0.5,
  user-scalable=yes
">
```

## 🔧 Адаптивные утилиты

### Скрыть на мобильных:
```css
.hide-mobile {
  display: none;
}

@media (min-width: 768px) {
  .hide-mobile {
    display: block;
  }
}
```

### Показать только на мобильных:
```css
.show-mobile {
  display: block;
}

@media (min-width: 768px) {
  .show-mobile {
    display: none;
  }
}
```

### Адаптивные отступы:
```css
.padding-responsive {
  padding: 15px;
}

@media (min-width: 768px) {
  .padding-responsive {
    padding: 30px;
  }
}
```

## 🎨 Адаптивная навигация

### Мобильное меню:
```css
.menu {
  display: none;
}

.menu-toggle {
  display: block;
}

@media (min-width: 768px) {
  .menu {
    display: flex;
  }
  
  .menu-toggle {
    display: none;
  }
}
```

## 📊 Адаптивные таблицы

```css
.table-responsive {
  overflow-x: auto;
}

@media (min-width: 768px) {
  .table-responsive {
    overflow-x: visible;
  }
}
```

## 🚀 Практические примеры

### 1. Мобильная навигация:
```css
/* Мобильные */
.nav {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
}

.menu-toggle {
  display: block;
}

/* Планшеты и больше */
@media (min-width: 768px) {
  .nav {
    display: flex;
    position: static;
  }
  
  .menu-toggle {
    display: none;
  }
}
```

### 2. Адаптивная сетка карточек:
```css
.cards {
  display: grid;
  gap: 20px;
  grid-template-columns: 1fr; /* 1 колонка */
}

@media (min-width: 640px) {
  .cards {
    grid-template-columns: repeat(2, 1fr); /* 2 колонки */
  }
}

@media (min-width: 1024px) {
  .cards {
    grid-template-columns: repeat(3, 1fr); /* 3 колонки */
  }
}

@media (min-width: 1280px) {
  .cards {
    grid-template-columns: repeat(4, 1fr); /* 4 колонки */
  }
}
```

### 3. Адаптивные колонки:
```css
.columns {
  display: flex;
  flex-direction: column; /* мобильные: вертикально */
  gap: 20px;
}

@media (min-width: 768px) {
  .columns {
    flex-direction: row; /* планшеты+: горизонтально */
  }
  
  .sidebar {
    flex: 0 0 250px; /* фиксированная ширина */
  }
  
  .content {
    flex: 1; /* остальное пространство */
  }
}
```

### 4. Адаптивные кнопки:
```css
.btn {
  padding: 10px 15px;
  font-size: 14px;
}

@media (min-width: 768px) {
  .btn {
    padding: 12px 24px;
    font-size: 16px;
  }
}

@media (min-width: 1024px) {
  .btn {
    padding: 15px 30px;
    font-size: 18px;
  }
}
```

## 📝 Шпаргалка

### Подходы:
- **Mobile First** — начинаем с мобильных, добавляем стили для больших экранов
- **Desktop First** — начинаем с десктопа, убираем/меняем для мобильных

### Ключевые техники:
1. Медиазапросы `@media`
2. Относительные единицы (`rem`, `%`, `vw`, `vh`)
3. Flexbox с `flex-wrap`
4. CSS Grid с `auto-fit/fill`
5. `max-width` для изображений и контейнеров

### Обязательные проверки:
- ✅ Viewport метатег
- ✅ Тестирование на реальных устройствах
- ✅ Контрастность текста на маленьких экранах
- ✅ Размеры кликабельных элементов (мин. 44×44px)

## 💡 Советы

1. Начинайте с Mobile First
2. Используйте относительные единицы
3. Тестируйте на реальных устройствах
4. Проверяйте performance на мобильных
5. Оптимизируйте изображения для разных разрешений