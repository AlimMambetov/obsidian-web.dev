# 🎯 CSS Псевдоэлементы - Краткий справочник

## 🎯 Основы

### Синтаксис:
```css
селектор::псевдоэлемент {
  свойства: значения;
}
```

**Важно:** Два двоеточия `::` (современный стандарт)

## 📍 Основные псевдоэлементы

### `::before` — контент ПЕРЕД элементом
```css
.element::before {
  content: "★ ";
  color: gold;
}
```
Результат: `★ Текст элемента`

### `::after` — контент ПОСЛЕ элемента
```css
.element::after {
  content: " →";
  color: blue;
}
```
Результат: `Текст элемента →`

## 📦 Свойство `content`

### Значения для `content`:
| Значение | Пример | Описание |
|----------|--------|----------|
| Текст | `content: "(";` | Обычный текст |
| Пустая строка | `content: "";` | Для декоративных элементов |
| Атрибут | `content: attr(data-text);` | Значение атрибута |
| Счетчик | `content: counter(item);` | Нумерация |
| Иконка | `content: "🎯";` | Emoji/символы |
| Изображение | `content: url(icon.png);` | Картинка |

```css
/* Примеры: */
.price::after {
  content: " руб.";
}

.link::after {
  content: " ↗";
}

.tooltip::before {
  content: attr(data-tooltip);
}
```

## 🎨 Практические примеры

### 1. Декоративные элементы:
```css
.quote::before {
  content: "“";
  font-size: 2em;
  color: #999;
}

.quote::after {
  content: "”";
  font-size: 2em;
  color: #999;
}
```

### 2. Иконки для ссылок:
```css
.external-link::after {
  content: " ↗";
  font-size: 0.9em;
}

.pdf-link::after {
  content: " 📄";
  margin-left: 5px;
}
```

### 3. Обязательные поля формы:
```css
.required::after {
  content: " *";
  color: red;
}
```

### 4. Декоративные линии:
```css
.title::before {
  content: "";
  display: block;
  width: 50px;
  height: 3px;
  background: blue;
  margin-bottom: 10px;
}
```

### 5. Номера списка:
```css
.steps li::before {
  content: counter(step) ". ";
  counter-increment: step;
  font-weight: bold;
  color: blue;
}

.steps {
  counter-reset: step;
}
```

## 📝 Текст и типографика

### `::first-letter` — первая буква
```css
p::first-letter {
  font-size: 2em;
  font-weight: bold;
  color: red;
  float: left;
  margin-right: 5px;
}
```

### `::first-line` — первая строка
```css
p::first-line {
  font-weight: bold;
  color: #333;
}
```

### `::selection` — выделенный текст
```css
::selection {
  background: yellow;
  color: black;
}

/* Для разных элементов: */
p::selection {
  background: lightblue;
}
```

## 📋 Списки и маркеры

### `::marker` — маркеры списков
```css
li::marker {
  color: red;
  font-size: 1.2em;
}

.custom-list li::marker {
  content: "✓ ";
  color: green;
}
```

### Стилизация маркеров:
```css
ul {
  list-style: none;
  padding-left: 0;
}

li::before {
  content: "• ";
  color: blue;
  margin-right: 10px;
}
```

## 🖼️ Изображения и видео

### `::placeholder` — текст-подсказка в полях
```css
input::placeholder {
  color: #999;
  font-style: italic;
}

textarea::placeholder {
  color: #666;
}
```

### Стилизация для состояний:
```css
input:focus::placeholder {
  color: transparent;
}
```

## 🎯 Декоративные элементы

### Треугольники:
```css
.tooltip::after {
  content: "";
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  border: 10px solid transparent;
  border-top-color: black;
}
```

### Кружки и точки:
```css
.dot::before {
  content: "";
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: green;
  margin-right: 10px;
}
```

### Разделители:
```css
.breadcrumb li::after {
  content: "/";
  margin: 0 10px;
  color: #999;
}

.breadcrumb li:last-child::after {
  content: "";
}
```

## 🔧 Полезные техники

### Очистка float:
```css
.clearfix::after {
  content: "";
  display: table;
  clear: both;
}
```

### Счетчики:
```css
.chapter {
  counter-reset: section;
}

.chapter h2::before {
  content: counter(section) ". ";
  counter-increment: section;
}
```

### Иконки в кнопках:
```css
.btn-download::before {
  content: "⬇";
  margin-right: 8px;
}

.btn-send::after {
  content: "✈";
  margin-left: 8px;
}
```

## 🎨 Градиенты и фоны

### Полоски:
```css
.striped::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: repeating-linear-gradient(
    45deg,
    transparent,
    transparent 10px,
    rgba(255,255,255,0.1) 10px,
    rgba(255,255,255,0.1) 20px
  );
  pointer-events: none;
}
```

### Затемнение:
```css
.image-wrapper::after {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    to bottom,
    transparent,
    rgba(0,0,0,0.7)
  );
}
```

## 📱 Адаптивные псевдоэлементы

### Разные иконки для разных экранов:
```css
.phone-link::before {
  content: "📞 ";
}

@media (min-width: 768px) {
  .phone-link::before {
    content: "Звоните: ";
  }
}
```

### Скрытие на мобильных:
```css
.decoration::before {
  content: "";
  /* стили для десктопа */
}

@media (max-width: 767px) {
  .decoration::before {
    display: none;
  }
}
```

## 🚀 Быстрые сниппеты

### Простая иконка:
```css
.icon-check::before {
  content: "✓";
  margin-right: 5px;
  color: green;
}
```

### Индикатор статуса:
```css
.status-active::before {
  content: "";
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: green;
  display: inline-block;
  margin-right: 8px;
}
```

### Цена с валютой:
```css
.price::before {
  content: "₽";
  margin-right: 2px;
}
```

### Звезды рейтинга:
```css
.rating::before {
  content: "★★★★★";
  color: gold;
  letter-spacing: 2px;
}
```

## ⚠️ Важные моменты

### Что можно стилизовать:
```css
.element::before {
  content: ""; /* обязательно! */
  display: block; /* или inline-block */
  position: absolute; /* или relative */
  width: 100px;
  height: 100px;
  background: blue;
  color: white;
  font-size: 16px;
  /* и другие свойства */
}
```

### Ограничения:
- Нельзя использовать в `<img>`, `<br>`, `<input>` (кроме placeholder)
- `content` может содержать только текст или изображения
- Не влияют на DOM (нельзя выбрать мышью)

## 📝 Шпаргалка

### Частые применения:
| Задача | Решение |
|--------|---------|
| Добавить иконку | `::before { content: "🔗"; }` |
| Декоративный элемент | `::after { content: ""; background: ... }` |
| Стилизация маркеров | `li::marker` или `li::before` |
| Кастомный placeholder | `input::placeholder` |
| Выделенный текст | `::selection` |

### Лучшие практики:
1. Всегда добавляйте `content` (даже пустой `""`)
2. Используйте `display` для контроля отображения
3. Для позиционирования используйте `position`
4. Тестируйте на разных браузерах
5. Не злоупотребляйте для важного контента (недоступность)

## 💡 Полезные советы

### Для доступности:
```css
/* Плохо: важный контент в псевдоэлементах */
.button::after {
  content: "Важная информация"; /* скринридер не прочитает */
}

/* Хорошо: только декоративные элементы */
.button::after {
  content: "→"; /* декоративная стрелка */
}
```

### Производительность:
```css
/* Плохо: много сложных псевдоэлементов */
.element::before,
.element::after,
.element .child::before {
  /* сложные анимации и трансформации */
}

/* Хорошо: минимальное количество простых элементов */
.element::after {
  content: "";
  /* простые стили */
}
```