Отлично! Ниже я собрал готовый файл `animations.ts` для работы со списками и элементами на странице. Он полностью на TypeScript, с поддержкой **index, direction, distance, stagger, scroll-trigger** и готов к импорту в любой компонент.

---

# 📁 animations.ts

```ts
// animations.ts
import { Variants } from "framer-motion";

/** -----------------------------
 * Общие анимации элементов
 * ----------------------------- */

// Fade In
export const fadeIn: Variants = {
  hidden: { opacity: 0 },
  visible: { opacity: 1, transition: { duration: 0.4, ease: "easeOut" } },
};

// Scale In (pop эффект)
export const scaleIn: Variants = {
  hidden: { opacity: 0, scale: 0.8 },
  visible: {
    opacity: 1,
    scale: 1,
    transition: { type: "spring", stiffness: 200, damping: 20 },
  },
};

/** -----------------------------
 * Анимации для списков с index и кастомными параметрами
 * ----------------------------- */

type Direction = "up" | "down" | "left" | "right";

interface ListItemOptions {
  direction?: Direction;
  distance?: number;
  duration?: number;
}

export const listItem = ({
  direction = "up",
  distance = 50,
  duration = 0.5,
}: ListItemOptions = {}): Variants => {
  const offsets = {
    up: { y: distance },
    down: { y: -distance },
    left: { x: distance },
    right: { x: -distance },
  };

  return {
    hidden: { opacity: 0, ...offsets[direction] },
    visible: (i: number) => ({
      opacity: 1,
      x: 0,
      y: 0,
      transition: {
        delay: i * 0.1,
        duration,
        ease: "easeOut",
      },
    }),
  };
};

// Контейнер с глобальным stagger
export const staggerContainer: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.15,
    },
  },
};

/** -----------------------------
 * Scroll-ready анимации
 * ----------------------------- */

// Fade Up при скролле
export const fadeUp: Variants = {
  hidden: { opacity: 0, y: 40 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.6, ease: "easeOut" },
  },
};

// Slide Up при скролле
export const slideUp: Variants = {
  hidden: { opacity: 0, y: 50 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: "easeOut" } },
};

// Hover preset (scale)
export const hoverScale = {
  whileHover: { scale: 1.05 },
  whileTap: { scale: 0.95 },
};

// Layout / FLIP transition
export const layoutTransition = {
  type: "spring",
  stiffness: 300,
  damping: 30,
};
```

---

# 📌 Использование в компоненте

```tsx
import { motion } from "framer-motion";
import { listItem, staggerContainer, fadeUp, hoverScale } from "@/animations";

const items = ["Первый", "Второй", "Третий", "Четвертый"];

export const AnimatedList = () => (
  <motion.ul
    variants={staggerContainer}
    initial="hidden"
    whileInView="visible"
    viewport={{ once: true }}
  >
    {items.map((item, i) => (
      <motion.li
        key={i}
        custom={i} // index для stagger
        variants={listItem({ direction: i % 2 === 0 ? "left" : "right", distance: 30 + i * 10 })}
        {...hoverScale} // hover эффект
      >
        {item}
      </motion.li>
    ))}
  </motion.ul>
);
```

✅ Что получаем:

1. **Список с stagger** — элементы появляются с небольшой задержкой.
    
2. **Кастомная анимация через index** — можно менять direction, distance и delay.
    
3. **Hover эффекты на каждом элементе**.
    
4. **Scroll trigger** — анимация срабатывает только при появлении на экране (`whileInView`).
    

---

Если хочешь, я могу сделать **расширенный пример**: целая **лендинг-секция с карточками**, где:

- Секция анимируется при скролле
    
- Каждая карточка имеет stagger, scale и flip-анимацию
    
- Все animation presets берутся из одного файла `animations.ts`
    

Это будет готовый boilerplate для продакшн-лендинга.

Хочешь, чтобы я так сделал?